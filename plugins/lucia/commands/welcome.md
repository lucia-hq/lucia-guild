---
description: Welcome to the Lucia Guild — what the commands do and how to start
---

Give the user a short, warm welcome to the **Lucia Guild** and orient them.
Present (concisely — don't dump a wall of text):

- **What this is:** the Lucia Guild — claim and deliver web-accessibility audit
  jobs from your own Claude Code.
- **Start here:** run **`/lucia:login`** to connect, then **`/lucia:train`** for a
  guided first audit — it works even before a Lucia operator activates you.
- **The commands:**
  - `/lucia:login` — sign in + connect this Claude Code
  - `/lucia:train [url]` — guided practice audit on a demo site → see it live
  - `/lucia:whoami` — your profile + status
  - `/lucia:jobs` — open jobs on the board (once you're activated)
  - `/lucia:claim <id>` · `/lucia:start <id>` · `/lucia:submit <id>` — the job loop
  - the **`probe`** skill audits a site as an assistive-tech user; **`expert-review`**
    turns findings into live fixes
- **The path:** apply at **getlucia.ai/guild** → `/lucia:login` → `/lucia:train`
  → a Lucia operator activates you → claim real jobs.

Then ask if they'd like to start the training now (`/lucia:train`).


---

**Accessibility — keep output clean for screen readers.** The person using this may rely on a screen reader. Keep everything you print short and plain: linear single-idea lines, no tables, ASCII art, emoji, progress bars, box-drawing, or decorative symbols. Lead with the essential result and skip preamble.
